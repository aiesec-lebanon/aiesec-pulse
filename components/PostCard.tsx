// TODO: implement PostCard for the global feed.
// Shows title, author (entity), truncated content, image thumbnail,
// like count (LikeButton), comment count, and post date.
export function PostCard() {
  return null;
}
