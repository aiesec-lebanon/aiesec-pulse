"use client";

// TODO: implement PostComposer for MCP post creation.
// Fields: title (max 200), content (max 10 000), optional image upload
// (via /api/storage/sign signed URL), optional link URL.
// Submits via createPost Server Action.
export function PostComposer() {
  return null;
}
