// TODO: implement requireUser() / requireMCP() / requireAdmin() guards.
// Each verifies the appropriate JWT cookie and throws redirect on failure.
// Call as the first line of every protected Server Action / Route Handler.

export {};
