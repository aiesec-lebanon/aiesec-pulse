// TODO: implement getSignedUploadUrl(filename, contentType, size).
// Validates MIME (image/jpeg | png | webp) and size (≤5MB), then calls the
// Supabase Storage admin API for a signed upload URL.
// Bucket: post-media (public read, server-side-only write).

export {};
